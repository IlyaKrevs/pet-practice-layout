import React from 'react'
import classes from './MainLogo.module.scss';

export const MainLogo = () => {
    return (
        <div className={classes.outer + ' ' + classes.circle}>
            <div className={classes.inner + ' ' + classes.circle}>

            </div>
        </div>
    )
}
